# 更新日志

本文档记录 LogcatX 的重要版本变更。

- English changelog: [`CHANGELOG.en.md`](./CHANGELOG.en.md)

## [0.7.0] - 2026-09-04

### 新增
- 设备更多菜单支持“截图当前画面到电脑剪切板”，可直接粘贴为图片
- 设备更多菜单新增 scrcpy 二级菜单，支持镜像设备画面或创建独立虚拟屏幕；新屏幕可跟随主屏，或指定竖屏 `720×1600` / `320 DPI`，并可填写或筛选启动包名
- 新增历史日志存储管理，可查看占用、按设备或时间范围预览并清理，同时保护仍在写入的日志
- 应用更新支持自动或自定义 HTTP / SOCKS5 代理，并在设置页提供代理配置检测

### 变更
- 设备更多菜单的“打开”更名为“打开最新日志文件”，意图更明确

### 修复
- 修复 Windows 发布包可能缺少程序图标和版本信息的问题

## [0.6.1] - 2026-08-27

### 修复
- 修复在输入框中使用中文输入法（如搜狗拼音）时，按回车确认候选或上屏原始拼音会导致输入框丢失焦点、内容可能丢失的问题；设备别名、logcat 参数、连接地址及设置页的所有单行输入框均已修复

## [0.6.0] - 2026-08-18

### 新增
- 应用内检查更新：点击侧边栏版本徽章可手动检查新版本、查看发行说明
- 自动检查更新（默认开启，可在设置页关闭）：每天本地时间 08:00 后首次打开窗口时静默检查一次，发现新版本会在版本徽章亮起提示并弹窗提醒
- 一键“下载并重启”完成升级：更新包经签名校验后自动安装并重启；新版本启动失败会自动回滚，不影响继续使用
- 更新提醒可点“稍后”跳过，直到更新的版本发布

### 变更
- 发行包改为单一绿色版 zip（内含主程序与文档），不再单独提供裸 exe；0.5.x 及更早版本需手动下载本版 zip 完成最后一次升级，之后即可在应用内更新
- 拖拽 APK / 文件到窗口时，悬停提示会显示当前默认拖拽目标设备，多设备时更不易误投

### 修复
- 修复设备别名包含中文等 Unicode 字符时，生成的日志目录与文件名出现乱码的问题
- 应用退出（包括异常退出）时确保所有 adb logcat 子进程一并终止，不再残留后台 adb 进程

## [0.5.3] - 2026-06-06

### 新增
- 设备更多菜单新增"复制最新日志文件路径"，可将当前或最近一次采集的日志文件路径复制到剪贴板
- 支持为每个设备单独配置 logcat 命令参数（如 `-v threadtime -s Tag:V *:E`），参数持久化保存，下次连接自动生效
- logcat 参数编辑弹窗，支持保存、清空操作

## [0.5.2] - 2026-05-12

### 新增
- 为主窗口增加最小尺寸约束，避免窗口被缩小到几乎无法使用

### 修复
- 清除当前前台 App 数据时，若直接执行 `pm clear` 因权限不足失败，会自动回退尝试 `run-as <package> pm clear <package>`
- 版本号升级为 `0.5.2`

## [0.5.1] - 2026-05-09

### 新增
- 设备默认显示名称升级为“别名 > 厂商+型号 > 序列号”
- 支持从设备列表识别当前前台 App
- 支持从设备列表对当前前台 App 执行强行停止、清除数据、卸载
- 为前台 App 的清除数据与卸载动作增加确认弹窗
- 支持将同一物理设备的 USB / Wi‑Fi ADB 连接自动识别并聚合为同一设备

### 变更
- 移除设备页中的“当前选中设备”详情区，设备页结构进一步收敛
- 设备别名编辑迁移到设备列表行的 `More` 菜单
- 设备列表说明文案改为强调“选中行作为默认拖拽目标 + More 菜单承接设备动作”
- 当同一设备同时存在 USB 和 Wi‑Fi transport 时，设备列表优先使用 USB 连接
- 版本号升级为 `0.5.1`

## [0.5.0] - 2026-05-06

### 新增
- 主界面重构为左侧导航栏 + 右侧主内容区布局
- 设备页底部增加固定高度（150px）日志面板，独立于主内容区滚动
- 支持从设备列表或详情区一键复制设备序列号
- 支持从设备列表或详情区直接打开指定设备的 Shell
- 支持从设备列表主动断开网络设备连接
- 支持在界面中一键重启 ADB Server
- 支持将 APK 直接拖入窗口安装到目标设备
- 支持将普通文件拖入窗口复制到设备 `/sdcard/Download`

### 变更
- 拖入文件时会优先投递到当前选中设备；未选中设备时会先弹出目标设备选择
- 支持一次处理多个拖入文件，并按 APK 安装 / 普通文件传输分别执行
- 移除顶部工具栏，将 GitHub 入口按钮整合到侧边栏底部
- 移除操作按钮栏中的"设置"和"清理历史日志"按钮（功能与侧边栏重复）
- 缩小概览统计卡片尺寸，优化信息密度
- 设备列表水平滚动条改为始终隐藏，支持滚轮横向滚动
- 空设备列表卡片按内容自然收缩，不再保留固定占位高度
- 版本号升级为 `0.5.0`

## [0.4.0] - 2026-04-29

### 新增
- 主界面增加 GitHub 项目主页入口按钮
- `config.json` 新增设备别名持久化、设备置顶和最近网络连接记录
- 支持直接输入 `IP:端口` 发起 `adb connect`，并提供最近连接记录弹窗
- 界面中的设备状态文案更友好
- 设备列表支持显示 Android 版本
- 首次启动时如果未自动检测到 ADB，会提供 Google 官方 Platform-Tools 下载链接

### 变更
- 设置别名后，设备日志会按别名目录归档
- 设置别名后，日志文件名前缀也会优先使用别名
- 修改设备别名时，会在可行情况下自动重命名历史日志目录
- 设备列表排序现在会优先展示已置顶设备
- 检测到 ADB 设备快照变化时，设备列表会自动刷新
- 设备行支持再次点击取消选中
- 版本号升级为 `0.4.0`

## [0.3.1] - 2026-04-24

### 变更
- 将共享桌面基础设施切换为公开的 `DeskFoundry` monorepo GitHub 依赖
- `desktop-logger`、`desktop-config`、`desktop-i18n` 和 `desktop-fs` 现在以可复用 SDK crate 的形式使用，不再保留应用内复制版本
- 版本号升级为 `0.3.1`

## [0.3.0] - 2026-04-23

### 新增
- 面向开源发布的 MIT License
- 内置英文与简体中文 UI 资源，并持久化保存语言选择
- 中文 README 与英文版 README 文档

### 变更
- 项目名称从 `adb-logcat-collector` 更名为 `LogcatX`
- 作为首个公开开源版本，版本号升级为 `0.3.0`
- 默认 Windows 发布产物统一使用 `LogcatX` 产品名
- 主界面更强调设备状态与快捷操作，而不再直接暴露原始文件路径
- 面向用户显示的 Windows 路径会规范化处理，不再直接显示 `\\?\` 前缀

### 打包
- 公开发布产物统一为 `LogcatX.exe` 和 `LogcatX-v0.3.0-win64.zip`

## [0.2.0] - 内部里程碑

### 新增
- 面向 Windows 的便携发布结构
- 便携配置路径解析（优先 exe 目录，其次 AppData）
- 独立的应用运行日志与 panic 捕获
- 通过 `build.rs`、图标资源和嵌入式 EXE 元数据完善 Windows 品牌信息
- 在界面中增加版本显示、配置/应用日志快捷入口，以及更丰富的设备会话信息
- 在 `plans/` 目录下加入版本规划文档
- 发布打包脚本与 `cargo xwin` 构建指引
- 内置英文与简体中文 UI 资源，并持久化保存语言选择

### 变更
- 默认版本号从 `0.1.0` 升级为 `0.2.0`
- Windows 构建默认改为 GUI subsystem，不再弹出控制台窗口
- 首次运行和设置流程更清楚地解释了配置路径、应用日志和便携模式
- 主界面更强调设备状态与快捷操作，而不再直接暴露原始文件路径
- 面向用户显示的 Windows 路径会规范化处理，不再直接显示 `\\?\` 前缀

### 打包
- 发布产物统一为一个 Windows 绿色版 zip，内含 exe、README、CHANGELOG 和配置示例
